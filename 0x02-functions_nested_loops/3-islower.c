#include "main.h"

/**
 * _islower - check for alphabet, in lowercase
 *@c: integer param
 *
 *Return: 1(true) or 0(false)
 */
int _islower(int c)
{

if (c >= 97)
{
return (1);
}
else
{
return (0);
}
}
