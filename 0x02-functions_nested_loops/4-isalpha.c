#include "main.h"

/**
 * _isalpha - check for alphabet, in lowercase
 *@c: integer param
 *
 *Return: 1(true) or 0(false)
 */
int _isalpha(int c)
{

if (c >= 65 || c >= 97)
{
return (1);
}
else
{
return (0);
}
}
